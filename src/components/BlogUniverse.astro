---
// BlogUniverse 组件 - 博客宇宙展示区域
import ScrollContainer from './ScrollContainer.astro';
import SidebarCard from './ui/SidebarCard.astro';

const blogs = [
  {
    id: 'flame',
    name: '焰 · Flame',
    emoji: '🔥',
    framework: 'Astro',
    url: 'https://sunyzhi55.github.io/astro-firefly-blog/',
    description: '焰起于心',
    color: 'from-[var(--color-fire)]/20 to-orange-500/20',
  },
  {
    id: 'flumina',
    name: '煴 · Flumina',
    emoji: '🪵',
    framework: 'VitePress',
    url: 'https://sunyzhi55.github.io/vitepress-teek-blog/',
    description: '煴藏于河',
    color: 'from-[var(--color-earth)]/20 to-amber-500/20',
  },
  {
    id: 'xi',
    name: '熹 · Xi',
    emoji: '🕯️',
    framework: 'Hugo',
    url: '#',
    description: '熹生于夜',
    color: 'from-purple-500/20 to-pink-500/20',
    coming: true,
  },
];
---

<SidebarCard title="博客宇宙" icon="🌌">
  <!-- 诗句 -->
  <p class="text-xs text-[var(--color-text-secondary)] italic mb-3 px-1 -mt-1">
    "焰起于心，煴藏于河，熹生于夜。"
  </p>

  <!-- 滚动容器 -->
  <ScrollContainer height="h-45" duration={18} maskTop={8} maskBottom={8}>
    {blogs.map((blog) => (
      <a 
        href={blog.coming ? undefined : blog.url}
        target={blog.coming ? undefined : "_blank"}
        rel="noopener noreferrer"
        class:list={[
          "blog-card block mb-2 p-3 rounded-xl transition-all duration-300",
          `bg-gradient-to-br ${blog.color}`,
          blog.coming ? "opacity-60 cursor-not-allowed" : "hover:scale-[1.02] cursor-pointer"
        ]}
      >
        <div class="flex items-center gap-2">
          <span class="text-2xl">{blog.emoji}</span>
          <div class="flex-1 min-w-0">
            <div class="flex items-center gap-2">
              <h4 class="font-medium text-sm text-[var(--color-text)] truncate">{blog.name}</h4>
              {blog.coming && (
                <span class="text-[10px] px-1.5 py-0.5 rounded-full bg-[var(--color-bg-secondary)] text-[var(--color-text-secondary)]">
                  即将
                </span>
              )}
            </div>
            <p class="text-xs text-[var(--color-text-secondary)]">{blog.description}</p>
          </div>
          <span class="text-[10px] px-1.5 py-0.5 rounded bg-[var(--color-card)] text-[var(--color-accent)]">
            {blog.framework}
          </span>
        </div>
      </a>
    ))}
  </ScrollContainer>
</SidebarCard>
