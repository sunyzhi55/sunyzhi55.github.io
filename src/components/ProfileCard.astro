---
// ProfileCard 组件 - 左侧个人信息卡片
const profile = {
  name: 'sunyzhi55',
  title: '前端开发者 / 创意技术人',
  avatar: 'https://avatars.githubusercontent.com/u/sunyzhi55',
  bio: '焰起于心，煴藏于河，熹生于夜。',
  location: '中国',
  email: 'sunyzhi55@example.com',
  social: [{ name: 'GitHub', url: 'https://github.com/sunyzhi55', icon: 'github' }],
};
---

<aside class="left-sidebar w-full xl:w-72 flex-shrink-0">
  <div class="sidebar sticky top-20 space-y-4">
    <!-- 个人信息卡片 -->
    <div class="profile-card card overflow-hidden relative">
      <!-- 全卡背景图 -->
      <div class="profile-bg" style="background-image: url('/avatar/circleBgImg.webp');"></div>
      <div class="profile-bg-overlay"></div>

      <!-- 装饰元素 -->
      <div class="absolute top-2 right-2 w-8 h-8 opacity-30 z-10">
        <svg viewBox="0 0 100 100" class="w-full h-full animate-rotate" style="animation-duration: 20s;">
          <path d="M50,10 A40,40 0 0,1 50,90 A20,20 0 0,1 50,50 A20,20 0 0,0 50,10" fill="var(--color-yang)" opacity="0.6"/>
          <path d="M50,10 A40,40 0 0,0 50,90 A20,20 0 0,0 50,50 A20,20 0 0,1 50,10" fill="var(--color-yin)" opacity="0.6"/>
        </svg>
      </div>
      
      <!-- 头像 -->
      <div class="px-4 pt-4 relative z-30">
        <div class="avatar-ring">
          <div class="w-20 h-20 rounded-full overflow-hidden bg-[var(--color-bg-secondary)]">
            <img src="/avatar/avatar.webp" alt="Avatar" class="w-full h-full object-cover" loading="lazy" />
          </div>
        </div>
      </div>

      <!-- 信息 -->
      <div class="p-4 pt-3 profile-content">
        <h2 class="text-lg font-black text-[var(--color-text)]">{profile.name}</h2>
        <p class="text-sm text-[var(--color-accent)] font-bold title-keep-format">{profile.title}</p>
        
        <p class="mt-3 text-sm text-[var(--color-text-secondary)] font-black leading-relaxed">
          {profile.bio}
        </p>

        <!-- 联系方式 -->
        <div class="mt-4 space-y-2">
          <div class="flex items-center gap-2 text-sm text-[var(--color-text-secondary)]">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
            </svg>
            <span class="font-black">{profile.location}</span>
          </div>
          
          <a 
            href={`mailto:${profile.email}`}
            class="flex items-center gap-2 text-sm font-black text-[var(--color-text-secondary)] hover:text-[var(--color-accent)] transition-colors"
          >
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
            </svg>
            <span class="font-black">{profile.email}</span>
          </a>
        </div>

        <!-- 社交链接 -->
        <div class="mt-4 pt-4 border-t border-[var(--color-border)]">
          <div class="flex items-center gap-3">
            <a 
              href="https://github.com/sunyzhi55"
              target="_blank"
              rel="noopener noreferrer"
              class="flex items-center gap-2 px-3 py-2 rounded-lg bg-[var(--color-bg-secondary)] hover:bg-[var(--color-bg)] text-[var(--color-text-secondary)] hover:text-[var(--color-accent)] transition-all flex-1 justify-center"
            >
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                <path fill-rule="evenodd" d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z" clip-rule="evenodd"/>
              </svg>
              <span class="text-sm font-black">GitHub</span>
            </a>
          </div>
        </div>
      </div>
    </div>

    <!-- 状态卡片 -->
    <div class="card p-4">
      <h3 class="text-sm font-black text-[var(--color-text-secondary)] mb-3 flex items-center gap-2">
        <span>📊</span> 状态
      </h3>
      <div class="space-y-3">
        <div class="flex items-center justify-between">
          <span class="text-sm text-[var(--color-text-secondary)]">当前状态</span>
          <span class="flex items-center gap-1.5 text-sm">
            <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
            <span class="text-[var(--color-text)]">在线</span>
          </span>
        </div>
        <div class="flex items-center justify-between">
          <span class="text-sm text-[var(--color-text-secondary)]">专注于</span>
          <span class="text-sm text-[var(--color-accent)]">Web 开发</span>
        </div>
        <div class="flex items-center justify-between">
          <span class="text-sm text-[var(--color-text-secondary)]">心情</span>
          <span class="text-sm">☕ 编码中...</span>
        </div>
      </div>
    </div>

    <!-- 技能标签 -->
    <div class="card p-4">
      <h3 class="text-sm font-black text-[var(--color-text-secondary)] mb-3 flex items-center gap-2">
        <span>🛠️</span> 技能
      </h3>
      <div class="flex flex-wrap gap-2">
        {['Astro', 'Vue', 'TypeScript', 'Tailwind', 'Node.js', 'Git'].map(skill => (
          <span class="px-2 py-1 text-xs rounded-full bg-[var(--color-bg-secondary)] text-[var(--color-accent)] border border-[var(--color-border)]">
            {skill}
          </span>
        ))}
      </div>
    </div>
  </div>
</aside>

<style>
  .profile-card {
    position: relative;
  }

  .profile-bg {
    position: absolute;
    inset: 0;
    background-size: cover;
    background-position: center;
    opacity: 0.66; /* 让背景图直接可见 */
    filter: none !important; /* 不受全局滤镜影响 */
    mix-blend-mode: normal !important;
    background-repeat: no-repeat !important;
    z-index: 0;
  }

  .profile-bg-overlay {
    position: absolute;
    inset: 0;
    background: transparent !important; /* 不覆盖或随主题变色 */
    z-index: 1;
    pointer-events: none;
  }

  /* 内容层：置于背景与覆盖层之上，但低于头像装饰 */
  .profile-card .profile-content {
    position: relative;
    z-index: 20;
  }

  .left-sidebar {
    min-width: 0;
  }

  @media (max-width: 1280px) {
    .left-sidebar {
      width: 100%;
    }
    
    .left-sidebar .sidebar {
      position: relative;
      top: 0;
      height: auto;
    }
  }

  /* 让卡片内文本可被选中，整体文本默认设为纯白色，但保留职称的原有样式 */
  .profile-card, .profile-card * {
    -webkit-user-select: text;
    -moz-user-select: text;
    -ms-user-select: text;
    user-select: text;
    
    color: #ffffff;
  }

  /* 确保链接等交互元素也可被选中 */
  .profile-card a, .profile-card button {
    -webkit-user-select: text;
    -moz-user-select: text;
    -ms-user-select: text;
    user-select: text;
  }

  /* 选区样式，便于在深色背景上看到选中效果 */
  .profile-card ::selection {
    background: rgba(255,255,255,0.18);
    color: #ffffff;
  }

  .profile-card .title-keep-format {
    color: var(--color-accent) !important;
  }
</style>
