---
// ProfileCard 组件 - 左侧个人信息卡片
import QuotesCard from './QuotesCard.astro';
import ContactItem from './ui/ContactItem.astro';
import SidebarCard from './ui/SidebarCard.astro';
import SkillTags from './ui/SkillTags.astro';
import StatusRow from './ui/StatusRow.astro';

const profile = {
  name: 'sunyzhi55',
  title: '代码爱好者 / 创意技术人',
  bio: '焰起于心，煴藏于河，熹生于夜。',
  location: 'China',
  email: 'sonaengji@gmail.com',
  github: 'https://github.com/sunyzhi55',
  skills: ['Astro', 'Vue', 'TypeScript', 'Tailwind', 'Node.js', 'Git', 'Python'],
};

// SVG 图标路径
const icons = {
  location:
    '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>',
  email:
    '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>',
  github:
    '<path fill-rule="evenodd" d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z" clip-rule="evenodd"/>',
};
---

<aside class="left-sidebar w-full xl:w-72 flex-shrink-0">
  <div class="sidebar sticky top-20 space-y-4 max-h-[calc(100vh-100px)] overflow-hidden">
    
    <!-- 个人信息卡片 -->
    <article class="profile-card card overflow-hidden relative">
      <div class="profile-bg" style="background-image: url('/avatar/circleBgImg.webp');"></div>
      
      <!-- 太极装饰 -->
      <svg viewBox="0 0 100 100" class="taiji-decor animate-rotate" style="animation-duration: 20s;">
        <path d="M50,10 A40,40 0 0,1 50,90 A20,20 0 0,1 50,50 A20,20 0 0,0 50,10" fill="var(--color-yang)" opacity="0.6"/>
        <path d="M50,10 A40,40 0 0,0 50,90 A20,20 0 0,0 50,50 A20,20 0 0,1 50,10" fill="var(--color-yin)" opacity="0.6"/>
      </svg>
      
      <!-- 头像 -->
      <figure class="avatar-wrapper">
        <div class="avatar-ring">
          <img src="/avatar/avatar.webp" alt="Avatar" class="avatar-img" loading="lazy" />
        </div>
      </figure>

      <!-- 信息内容 -->
      <div class="profile-content">
        <h2 class="text-lg font-black">{profile.name}</h2>
        <p class="text-sm font-bold title-accent">{profile.title}</p>
        <p class="mt-3 text-sm font-black leading-relaxed opacity-90">{profile.bio}</p>

        <!-- 联系方式 -->
        <address class="mt-4 space-y-2 not-italic">
          <ContactItem icon={icons.location}>{profile.location}</ContactItem>
          <ContactItem href={`mailto:${profile.email}`} icon={icons.email}>{profile.email}</ContactItem>
        </address>

        <!-- GitHub 链接 -->
        <nav class="mt-4 pt-4 border-t border-white/20">
          <a href={profile.github} target="_blank" rel="noopener noreferrer" class="github-link">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
              <Fragment set:html={icons.github} />
            </svg>
            <span class="text-sm font-black">GitHub</span>
          </a>
        </nav>
      </div>
    </article>

    <!-- 状态卡片 -->
    <SidebarCard title="状态" icon="📊">
      <div class="space-y-3">
        <StatusRow label="当前状态">
          <span class="flex items-center gap-1.5 text-sm">
            <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
            <span class="text-[var(--color-text)]">在线</span>
          </span>
        </StatusRow>
        <StatusRow label="专注于">
          <span class="text-sm text-[var(--color-accent)]">Web 开发</span>
        </StatusRow>
        <StatusRow label="心情">
          <span class="text-sm">☕ 编码中...</span>
        </StatusRow>
      </div>
    </SidebarCard>

    <!-- 技能标签 -->
    <SidebarCard title="技能" icon="🛠️">
      <SkillTags skills={profile.skills} />
    </SidebarCard>

    <!-- 名言励句卡片 -->
    <QuotesCard />
  </div>
</aside>

<style>
  .profile-card {
    --card-text: #ffffff;
  }

  .profile-bg {
    position: absolute;
    inset: 0;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    opacity: 0.66;
    z-index: 0;
  }

  .taiji-decor {
    position: absolute;
    top: 0.5rem;
    right: 0.5rem;
    width: 2rem;
    height: 2rem;
    opacity: 0.3;
    z-index: 10;
  }

  .avatar-wrapper {
    padding: 1rem 1rem 0;
    position: relative;
    z-index: 30;
    margin: 0;
  }

  .avatar-ring {
    padding: 3px;
    background: var(--gradient-primary);
    border-radius: 50%;
    display: inline-block;
  }

  .avatar-img {
    width: 5rem;
    height: 5rem;
    border-radius: 50%;
    object-fit: cover;
    background: var(--color-bg-secondary);
  }

  .profile-content {
    padding: 0.75rem 1rem 1rem;
    position: relative;
    z-index: 20;
    color: var(--card-text);
  }

  .title-accent {
    color: var(--color-accent) !important;
  }

  .github-link {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.5rem 0.75rem;
    border-radius: 0.5rem;
    background: rgba(255, 255, 255, 0.1);
    color: var(--card-text);
    transition: all 0.2s ease;
  }

  .github-link:hover {
    background: rgba(255, 255, 255, 0.2);
    color: var(--color-accent);
  }

  /* 文本样式 */
  .profile-card, .profile-card * {
    user-select: text;
    color: var(--card-text);
  }

  .profile-card ::selection {
    background: rgba(255, 255, 255, 0.18);
  }

  /* 侧边栏响应式 */
  .left-sidebar {
    min-width: 0;
  }

  @media (max-width: 1280px) {
    .left-sidebar {
      width: 100%;
    }
    
    .left-sidebar .sidebar {
      position: relative;
      top: 0;
      height: auto;
      max-height: none;
    }
  }
</style>
